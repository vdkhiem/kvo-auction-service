AuctionsTableIAM:
  Effect: Allow
  Action: 
    - dynamodb:PutItem
    - dynamodb:Query
    - dynamodb:Scan
    - dynamodb:GetItem
    - dynamodb:UpdateItem
  Resource:
    - ${self:custom.AuctionsTable.arn}
    #- arn:aws:dynamodb:#{AWS::Region}:#{AWS::AccountId}:table/AuctionsTable