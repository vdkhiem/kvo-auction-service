AuctionsTableIAM:
  Effect: Allow
  Action: 
    - dynamodb:PutItem
    - dynamodb:Query
    - dynamodb:Scan
    - dynamodb:GetItem
  Resource:
    - ${self:custom.AuctionsTable.arn}
    #- arn:aws:dynamodb:#{AWS::Region}:#{AWS::AccountId}:table/AuctionsTable